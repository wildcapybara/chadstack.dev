name: Analytics Web Deploy
on:
  workflow_dispatch:
  push:
    branches: [master]
  path: ["platforms/analytics/**"]

jobs:
  Deploy:
    uses: ./.github/workflows/reusable-deployment-docker.yaml
    with:
      app_name: analytics
      app_path: platforms/analytics
      namespace: platforms
