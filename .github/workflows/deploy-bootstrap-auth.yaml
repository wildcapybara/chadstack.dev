name: Auth Deploy
on:
  push:
    branches: [master,stage]
  paths: ["bootstrap/auth/**"]

jobs:
  Deploy:
    uses: ./.github/workflows/reusable-deployment-docker.yaml
    with:
      app_name: auth
      app_path: bootstrap/auth
      namespace: bootstrap
