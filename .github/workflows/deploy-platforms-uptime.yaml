name: Uptime Deploy
on:
  workflow_dispatch:
  push:
    branches: [master]
  path: ["platforms/uptime/**"]

jobs:
  Deploy:
    uses: ./.github/workflows/reusable-deployment-docker.yaml
    with:
      app_name: uptime
      app_path: platforms/uptime
      namespace: platforms
