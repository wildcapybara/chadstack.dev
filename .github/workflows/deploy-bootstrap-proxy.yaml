name: Proxy Deploy
on:
  workflow_dispatch:
  push:
    branches: [master]
  path: ["bootstrap/proxy/**"]

jobs:
  Deploy:
    uses: ./.github/workflows/reusable-deployment-docker.yaml
    with:
      app_name: proxy
      app_path: bootstrap/proxy
      namespace: bootstrap
