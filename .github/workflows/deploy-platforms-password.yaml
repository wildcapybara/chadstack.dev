name: Password Deploy
on:
  workflow_dispatch:
  push:
    branches: [master]
  path: ["platforms/password/**"]

jobs:
  Deploy:
    uses: ./.github/workflows/reusable-deployment-docker.yaml
    with:
      app_name: password
      app_path: platforms/password
      namespace: platforms
