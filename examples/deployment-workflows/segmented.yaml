name: AppName Deploy
on:
  workflow_dispatch:
  push:
    branches: [master, stage, develop]
  path: ["appName/**"]

jobs:
  Deployment:
    uses: ./.github/workflows/reusable-deployment-docker.yaml
    with:
      namespace: examples
      app_name: segmented
      server_name: self-hosted-${{ github.ref_name }}
